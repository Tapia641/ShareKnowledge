    "bcrypt-nodejs": "0.0.3", //Encriptar passwords --save
    "body-parser": "^1.19.0", //Convertir a objeto usable por javascript --save
    "connect-multiparty": "^2.2.0", //Subir archivos --save
    "express": "^4.17.1", //Framework http para rutas --save
    "jwt-simple": "^0.5.6", //Gestionar tokens & autentifacion --save
    "moment": "^2.24.0", //Fechas --save
    "mongoose": "^5.8.4" //ORM MongoDB en NodeJS --save

  "devDependencies": {
    "nodemon": "^2.0.2" //Refrescar el servidor ante los cambios --save-dev